#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'

# help: Open the AWS console via aws-vault
#
# Usage:
#   dev aws-login <profile>
#
# Arguments are passed straight through to "aws-vault login".

if [ "${1:-}" = "-h" ] || [ "${1:-}" = "--help" ]; then
    exec "$(dirname "$0")/dev" help "${0##*/dev-}"
fi

exec aws-vault login -- "$@"
