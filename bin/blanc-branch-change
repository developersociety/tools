#!/bin/bash
set -e

if [ -z "$VIRTUAL_ENV" ]; then
    echo "Must be in a virtual env"
    exit 1
fi

npm install
pip install -r requirements/local.txt
fab get_backup
fab get_media
./manage.py migrate
